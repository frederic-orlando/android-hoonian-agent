package com.example.hoonianAgent.presenter.callback;

import com.example.hoonianAgent.model.content.property.Cluster;

public interface ClusterRecyclerListener {
    void openClusterDetail(Cluster cluster);
}
