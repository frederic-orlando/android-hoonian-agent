package com.example.hoonianAgent.presenter.callback;

import com.example.hoonianAgent.model.content.referred.Referred;

public interface ReferredFormCallback {
    void refresh();
    void addNotes(int position);
}
